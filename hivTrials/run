#!/usr/bin/env bash

for ((i=0; i < 10; i++))
do
    cd hiv$i/FastTree/RAxML
    rm *
    cp ../../trueData/hivTrueMSA.fasta .
    cat ../fast.tre | resolve_random.py > resolved.tre
    raxml-ng --msa hivTrueMSA.fasta --model GTR+FO+I+R4 --evaluate --tree resolved.tre
done
