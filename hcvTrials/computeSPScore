#!/usr/bin/env bash

for ((i=0; i < 10; i++))
do
    java -jar ../helperScripts/FastSP.jar -r hcv$i/trueData/hcvTrueMSA.fasta -e hcv$i/MAFFT/MAFFT.aln | grep SP-Score | sed 's/SP-Score//' >> MAFFTSP
done

for ((i=0; i < 10; i++))
do
    java -jar ../helperScripts/FastSP.jar -r hcv$i/trueData/hcvTrueMSA.fasta -e hcv$i/MUSCLE/MUSCLE.aln | grep SP-Score | sed 's/SP-Score//' >> MUSCLESP
done

for ((i=0; i < 10; i++))
do
    java -jar ../helperScripts/FastSP.jar -r hcv$i/trueData/hcvTrueMSA.fasta -e hcv$i/ClustalOmega/Clustal.aln | grep SP-Score | sed 's/SP-Score//' >> ClustalSP
done
